/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FeetPipe } from './feet.pipe';

describe('Pipe: Feet', () => {
  it('create an instance', () => {
    let pipe = new FeetPipe();
    expect(pipe).toBeTruthy();
  });
});
